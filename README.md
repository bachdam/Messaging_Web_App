We need to open 2 separate terminals: one for client and one for server. 
Client:
    Use this command to run the client site: npm run dev
Server: 
    Use this command to eun the server site: node index.js
